<% if object && object.errors.size > 0 %>
  <div id="errorExplanation">
    <h2 class="action"><%= pluralize(object.errors.size, "error") %> prohibited this record from being saved</h2>
   
    <ul>
    <% object.errors.full_messages.each do |msg| %>
      <li class='action'><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
